import { UserSettings, Integration, Subscription } from './typings'

export const MOCK_USER_SETTINGS: UserSettings | null = null

export const MOCK_INTEGRATIONS: Integration[] = []

export const MOCK_SUBSCRIPTION: Subscription | null = null
